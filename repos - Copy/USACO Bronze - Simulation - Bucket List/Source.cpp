#include <iostream>
#include <vector>
#include <algorithm>
int main()
{
	int num;
	std::cin >> num;
	std::vector<int> bucket_list;
	std::vector<int> type;
	std::vector<int> time;

	for (int i = 0; i < num; i++)
	{
		int a, b, c;
		std::cin >> a >> b >> c;
		time.push_back(a);
		time.push_back(b);
		type.push_back(a);
		type.push_back(b);

	}
	std::sort(list.begin(), list.end());

}